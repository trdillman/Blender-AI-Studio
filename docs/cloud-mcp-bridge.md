# Cloud MCP Bridge

This is optional.

Set:

```text
REMOTE_MCP_GATEWAY_URL=https://linkedmcp.qzz.io/mcp
```

Use it only when cloud-side Codex needs access to the already-exposed local MCP endpoint. Keep GitHub as the primary collaboration path.
