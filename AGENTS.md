<!-- codex-managed:start -->
# Codex Rules

## Codex Cloud Addendum

- Keep GitHub as the primary collaboration path.
- Use `REMOTE_MCP_GATEWAY_URL` only for optional access to already-exposed local MCP tools.
- Do not assume the MCP bridge makes GUI Blender a cloud-native workflow.

<!-- codex-managed:end -->
